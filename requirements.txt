# Backward-compatible default: the original SurgTPGS/4DGS environment.
# For the integrated pipeline, install each environment separately; see
# requirements/README.md and the top-level README.md.
-r requirements/surgtpgs.txt
